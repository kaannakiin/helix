// ─── Enums ────────────────────────────────────────────────────────────────────

enum AccountStatus {
  ACTIVE
  SUSPENDED
  BANNED
  DEACTIVATED
}

enum DeviceType {
  DESKTOP
  MOBILE
  TABLET
  UNKNOWN
}

enum LoginMethod {
  EMAIL
  PHONE
  OAUTH_GOOGLE
  OAUTH_GITHUB
  OAUTH_APPLE
  OAUTH_FACEBOOK
  OAUTH_INSTAGRAM
  TWO_FACTOR
}

enum OAuthProvider {
  GOOGLE
  FACEBOOK
  INSTAGRAM
}

enum LoginStatus {
  SUCCESS
  FAILURE
}

enum AccountEventType {
  LOGIN
  LOGOUT
  PASSWORD_CHANGE
  EMAIL_CHANGE
  PHONE_CHANGE
  TWO_FACTOR_ENABLED
  TWO_FACTOR_DISABLED
  ACCOUNT_DEACTIVATED
  ACCOUNT_REACTIVATED
  ACCOUNT_SUSPENDED
  ACCOUNT_BANNED
  PROFILE_UPDATED
  DEVICE_TRUSTED
  DEVICE_REMOVED
  SESSION_REVOKED
  PASSWORD_RESET_REQUESTED
  PASSWORD_RESET_COMPLETED
  EMAIL_VERIFIED
  PHONE_VERIFIED
}

enum SessionRevokeReason {
  USER_LOGOUT
  USER_REVOKED
  ADMIN_REVOKED
  PASSWORD_CHANGED
  SECURITY_CONCERN
  TOKEN_ROTATION_ANOMALY
  EXPIRED
}

// ─── i18n ─────────────────────────────────────────────────────────────────────

enum Locale {
  EN
  TR
}

// ─── Currency ─────────────────────────────────────────────────────────────────

enum CurrencyCode {
  TRY
  USD
  EUR
  GBP
}

// ─── Product ──────────────────────────────────────────────────────────────────

enum ProductType {
  PHYSICAL
  DIGITAL
}

enum ProductStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum TrackingStrategy {
  NONE
  BATCH
  SERIAL
  BATCH_AND_SERIAL
}

// ─── File ─────────────────────────────────────────────────────────────────────

enum FileType {
  IMAGE
  VIDEO
  DOCUMENT
  OTHER
}

// ─── Inventory ────────────────────────────────────────────────────────────────

enum StockCategory {
  UNRESTRICTED
  QUALITY_CHECK
  BLOCKED
  IN_TRANSIT
  RESERVED
}

enum MovementType {
  GOODS_RECEIPT
  GOODS_RECEIPT_REVERSAL
  GOODS_ISSUE
  GOODS_ISSUE_REVERSAL
  TRANSFER_OUT
  TRANSFER_IN
  STOCK_ADJUSTMENT_IN
  STOCK_ADJUSTMENT_OUT
  RETURN_FROM_CUSTOMER
  RETURN_TO_SUPPLIER
  QUALITY_RELEASE
  QUALITY_BLOCK
  SCRAP
}

enum StockMovementStatus {
  DRAFT
  POSTED
  CANCELLED
}

// ─── Variant ──────────────────────────────────────────────────────────────────

enum VariantGroupType {
  COLOR
  SIZE
}

enum VariantGroupDisplayMode {
  BADGE
  SELECT
  IMAGE_GRID
}

// ─── Warehouse ────────────────────────────────────────────────────────────────

enum WarehouseStatus {
  ACTIVE
  INACTIVE
  MAINTENANCE
}

enum BinType {
  STORAGE
  RECEIVING
  SHIPPING
  QUALITY
  DAMAGE
  RETURN
}

// ─── Geolocation ─────────────────────────────────────────────────────────────

enum CountryType {
  HAS_STATES
  HAS_CITIES_ONLY
  COUNTRY_ONLY
}

// ─── Pricing ────────────────────────────────────────────────────────────────

enum PriceListType {
  BASE
  SALE
  CUSTOM
}

enum PriceListStatus {
  ACTIVE
  DRAFT
  ARCHIVED
}

enum PriceOriginType {
  FIXED
  RELATIVE
}

enum AdjustmentType {
  PERCENTAGE
  FIXED_AMOUNT
}

// ─── Rule Engine ──────────────────────────────────────────────────────────────

enum RuleTargetEntity {
  USER
  PRODUCT
  ORDER
  INVENTORY
}

enum CustomerGroupType {
  RULE_BASED
  MANUAL
}

// ─── Evaluation ───────────────────────────────────────────────────────────────

enum EvaluationJobStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  CANCELLED
}

enum EvaluationTrigger {
  SCHEDULED
  MANUAL
}

// ─── Store ────────────────────────────────────────────────────────────────────

enum BusinessModel {
  B2C
  B2B
}

enum StoreStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum StorefrontStatus {
  PENDING_HOST
  ACTIVE
  SUSPENDED
}

enum InstallationStatus {
  ACTIVE
  SUSPENDED
}

enum DomainOnboardingMode {
  EXACT_HOSTS
  WILDCARD_DELEGATION
  HYBRID
}

enum DomainSpaceStatus {
  PENDING_OWNERSHIP
  READY
  FAILED
  ARCHIVED
}

enum DomainOwnershipMethod {
  TXT_TOKEN
  DIRECT_DNS
}

enum DomainRoutingMethod {
  HTTP_WELL_KNOWN
  DIRECT_DNS
}

enum VerificationStatus {
  PENDING
  VERIFIED
  FAILED
  SKIPPED
}

enum HostBindingType {
  PRIMARY
  ALIAS
}

enum HostBindingStatus {
  PENDING_ROUTING
  ACTIVE
  FAILED
  DISABLED
}

// ─── Account ──────────────────────────────────────────────────────────────────

enum AccountType {
  PERSONAL
  BUSINESS
}

enum OrgMemberRole {
  OWNER
  MANAGER
  MEMBER
}
